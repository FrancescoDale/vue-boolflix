var app = new Vue ({
    el : '#root',
    data : {
        inputSearch : '',
        moviesList : []
    },
    methods : {
        //controllo if per verificare che l'input non sia una stringa vuota
        if
    },
    mounted() {},
});
